import { BankersRoundigPipe } from './bankers-roundig.pipe';

describe('BankersRoundigPipe', () => {
  it('create an instance', () => {
    const pipe = new BankersRoundigPipe();
    expect(pipe).toBeTruthy();
  });
});
